TODO
in the css-cleanup-tailwind-upgrade branch finish the checkout progress fix. Also check other places where conditional tailwind css is displayed.

furthermore push the personal details branch.


---
~~refactor/css-cleanup-tailwind-upgrade~~

This is the initial message of the MR
```
Other refactor/css-cleanup-tailwind-upgrade-* branches are at the moment pulling from this branch.

This is the initial MR for the tailwind upgrade. In the older tailwind version, there was a bug that allowed the later set tailwind inline classes to override the earlier set ones. In the current version this is no longer the case. There might still be places where the ui  changes because this bug is fixed now. In my initial sweep I only found one instance of th

```