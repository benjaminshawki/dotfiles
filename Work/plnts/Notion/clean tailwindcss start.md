When working with classes in Tailwind CSS, it's essential to avoid splitting up these classes across multiple strings. Doing so causes the ordering plugin to sort individual parts of the string instead of the collection of classes, leading to inconsistencies and potential issues in the final output. Instead, keep related classes together, either in a single or multi line with double quotes. This approach ensures that the plugin can properly order the classes and this helps maintain readability and maintainability.