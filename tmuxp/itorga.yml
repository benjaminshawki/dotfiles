session_name: itorga

windows:
  - window_name: Notes
    start_directory: $HOME/Notes/Studies/IN2/ITORGA/
    layout: tiled
    panes:
      - nvim -c "lua require('telescope.builtin').find_files()"
