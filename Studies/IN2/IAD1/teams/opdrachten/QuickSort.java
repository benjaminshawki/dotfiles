/**
 * @author Charlotte Ipema
 * @since September 2017
 */

public class QuickSort {

	public void quickSort(int[] array) {
		quickSort(array, 0, array.length - 1);
		
	}

	public void quickSort(int[] array, int lo, int hi) {
		if lo < hi
			hi is je pivot, die wil je op de juiste plek zetten 
				int p = [partition(array, lo, hi)
			quicksort(array, lo, p-1)
			quicksort(array, p+1, hi)
	}
	
	partition(array, lo, hi)
		kies een pivot, bv array[hi]
		zoek nu de juiste plek voor je pivot
		
		??
		
		return index van de pivot
}
