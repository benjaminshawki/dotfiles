`ext2` linux upto 1990's 32TiB - TiB

`ext3` Journaling Jourld 32 TiB - 2TiB

`ext4` 1EiB, 16 TiB

`ReiserFs` 32kB

`JFS` used by IBM journaled file AIX os Ost2

`XFS` Silicon graphics IRIX Os

`Btrfs` large files and disks

`FAT`  Dos Windows 16, 32

`NTFS` NT Xor Xil 

`MACOS` HFS / HFSTV

`sudo mkfs -t vfat /dev/sdb1` or `sudo mkfs.vfat /dev/sdb1`

`sudo parted /dev/sdb`

`sudo mkswap /dev/sdb1`

`sudo swapon /dev/sdb1`


