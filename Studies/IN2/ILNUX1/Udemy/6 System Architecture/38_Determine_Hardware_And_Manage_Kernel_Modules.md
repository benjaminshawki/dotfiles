Managing hardware

>`lspci` 
>

`-v` = verbose for detail

> `lsusb`

> `modprobe` 
>
> `-n` for dry run
> 
`lsmod`

`modinfo`

`modprobe -r e1000` to remove module named e1000

Drivers are needed for linux, here they are called modules

for modprobe configuration files -> `/etc/modprobe.d$`
