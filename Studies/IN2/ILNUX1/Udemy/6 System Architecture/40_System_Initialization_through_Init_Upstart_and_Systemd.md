> **Init**
> Father of all processes is `init` process around the time of `system V` it was also part of the Linux Standard Base `LSB`
>
> `/etc/init/tab` 
>
> not a modern process
>
> **Upstart**
> upstart.ubuntu.com
>
> `/etc/init` `initctl`
>
> **Systemd**
>
> `systemd` used on fedora, opensuse, archlinux
>
> `/etc/systemd` <- prefer editing this file and not the next
>
> `/user/lib/systemd/sytem`
>

`sudo systemctl` 
