`/dev` for devices

if on `ls -l` the first character in the permissions column is `c` th file is character type `b` type is block type.

`sda` disk partition

> `PATA` Parallel Advanced Technology Attachment 
> 
> popular until 2005 but has since been replaced by `SATA`
>
> ---
> 
> `SATA` Serial Parallel Advanced Technology Attachment
> 
> ---
> 
> `SCSI` Small Computer System Interface
>
> 