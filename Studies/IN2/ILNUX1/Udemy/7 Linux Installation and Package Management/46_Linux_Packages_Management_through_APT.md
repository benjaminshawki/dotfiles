`APT`

`/etc/sources.list`

`/etc/source.list.d/`

Cash is maintained and is checked first before outgoing request to ubuntu repositories

>`apt-get` 
>
>>`install` `remove` `purge` `update` `upgrade` `dist-upgrade`

`sudo add-ap-repository`

`sudo apt-ache search gcalctool` & `sudo apt-ache show gcalctool`

`sudo dpkg-reconfigure`

**aptitude**








