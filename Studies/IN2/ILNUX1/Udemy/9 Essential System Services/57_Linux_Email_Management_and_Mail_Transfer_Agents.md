`SMTP` Push

`SMTP` servers are also known as `MTA` Mail Transfer Agents

`POP` Post office Protocol

`IMAP` Internet message protocol

>`Sendmail` old
>
>`PostFix` Secure, modern, exim.ortg
>
>`Exim` also moden but monolithic, easy to configure
> 
> `Qmail` modern, secure 
