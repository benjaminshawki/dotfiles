> date
> `sudo date -s 10/10/1999 +10:00:00`
> 
> `sudo hwclock --hctosys`
> 
> sudo hwclock --systohc

> `sudo hwclock` hardware bios clock
> 
> `sudo hwclock --set --date="10/10/2001 10:00`

>NTP -> Network transfer protocol
>
> Atomic logs / Atomic sources
> 
> 


> www.pool.ntp.org/
> 
> `ntpdate`