`XDMCP` 


`XDM` /etc/x11/xdm/xdm-config

`GDM` /etc/x11/gdm or /etc/gdm GENOME default RedHat Fedora CentOs

`KDM` /etc/x11/kdm or /etc/kde/kdm or /usr/lib/kde4/libexec KDE

`LightDM`  /etc/lightdm/lightdm.conf default debian

`MDM`

`systemctl | grep -i display`

`systemctl disable lightdm.service`

`sudo service lightdm restart`

`sudo dpkg-reconfigure $` $ = lightdm or gde or etc.

`xwininfo`


