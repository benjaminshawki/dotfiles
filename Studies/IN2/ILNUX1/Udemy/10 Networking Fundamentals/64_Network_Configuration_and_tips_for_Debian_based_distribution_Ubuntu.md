>`ifconfig`
> 
> `sudo ifconfig eth0 192.168.1.21 netmask 255.255.255.0` 

>`ifup`
> 
> 

`ifdown`

`hostname`

> `route`
> 
> `route -n`
> 
> `sudo route add default gw 192.168.1.1`


`nano /etc/network/interfaces`

`traceroute -n 4.2.2.3`

`sudo nc -l 12345` `echo 'this is SOS message' | nc 192.168.1.25 12345`

`sudo tcpdump -i eth0` Wireshark is a very usefull tool

`sudo nano /etc/hostname`

`sudo nano /etc/hosts`


