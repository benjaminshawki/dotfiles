/**
 * @param {string} s
 * @return {number}
 */
var romanToInt = function(s) {
	const I =
		let numerals = {I = 1, V = 5, X , L, C, D, M};
	let sum = 0;
	let

		for (let i = 0; i < s.length; i++) {
		if (s[s.length - 1 - i])
			if (numerals[s.length - i] % 2 === 1) {

			}
	}
};