`vim /etc/locale.gen`

`locale-gen`

`echo LANG=en_US.UTF-8 > /etc/locale.conf`

`export LANG=en_US.UTF-8`
 
`echo t3-arch > /etc/hostname`

`vim /etc/hosts`

/etc/hosts
```
127.0.0.1   localhost
::1         localhost
127.0.0.1   t3-arch.localdomain t3-arch
```

`mkinitcpio -P`