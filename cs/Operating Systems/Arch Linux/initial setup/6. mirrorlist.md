`curl -L -o mirrorlist "https://archlinux.org/mirrorlist/?country=NL&protocol=https&ip_version=4"`

`sudo tee -a /etc/pacman.d/mirrorlist < mirrorlist`

`sudo pacman -Syy`

uncomment mutli in /etc/pacman.conf