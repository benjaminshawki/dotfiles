# let, const, var

let  
     Variable that changes thoughout the program  
        "Reassings"
        "Mutates"
        
const
    Variable cannot be changed
    Imutatable variable
    
var
    should never be used
    only use for legacy code
    